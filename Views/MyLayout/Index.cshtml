<a data-toggle="dropdown" href="#">
    <span class="glyphicon glyphicon-bell text-xxl-center"> @ViewBag.notifiCount </span>
</a>
<ul class="dropdown-menu overflow-auto" data-bs-spy="scroll" data-bs-offset="20" style="max-height: 200px; width:250px">
    @foreach (var category in ViewBag.notifi)
    {
    <li>

        @if (category.Message.Contains("Oceń"))
        {
            <p >
                <a href="@Url.Action("DeleteAndViewProfile", "MyLayout", new { idNotifications = category.ID, idProfile = category.Offer.Profile.ID })"> @category.Message</a>
            </p>
        }
        else
        {
            <p >
                <a href="@Url.Action("DeleteAndViewOffer", "MyLayout", new { idNotifications = category.ID, idOffer = category.Offer.ID })"> @category.Message</a>
            </p>
        }
    </li>
    }

</ul>